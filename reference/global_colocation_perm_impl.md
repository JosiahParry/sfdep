# Global Colocation Quotient Conditional Permutation Implementation

This function will repeat calculation of the global colocation quotient
(CLQ) a specified number of times and return a simulated p-value.

## Usage

``` r
global_colocation_perm_impl(A, nb, nsim = 99)
```
